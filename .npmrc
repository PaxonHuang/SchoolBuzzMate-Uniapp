# SchoolBuzzMate Uniapp pnpm 配置

# 用 hoisted 布局而非默认 symlink 布局 (2026-07-19 决策)
# 原因: HBuilderX GUI 在 WSL2 跨边界 (\\wsl$\... 9P 驱动) 场景下,
# 看不惯 pnpm 默认的 symlink 布局 — "运行 → 微信开发者工具" 会报
# "node_modules缺少编译器模块"。hoisted 模式把所有包铺平为真实目录,
# HBuilderX / 微信开发者工具 / CLI 三方都能直接走 npm 风格 require 解析。
#
# 代价: node_modules 体积约 3-5x (每个包独立副本, 不再 symlink 共用 .pnpm/),
# 但本项目编译产物仅 828K, 可接受。
node-linker=hoisted